// Este archivo ya no se utiliza ya que los datos se obtienen directamente de Supabase
// Se mantiene solo como referencia histórica

import { UFData, GlobalStats, Alert } from "../types";

// Datos mock desactivados
export const globalStats: GlobalStats = {
  totalContractual: 0,
  totalExecuted: 0,
  globalExecution: 0,
  activeUFs: 0,
  totalUFs: 0,
};

// Datos mock desactivados
export const ufData: UFData[] = [];

// Datos mock desactivados
export const allAlerts: Alert[] = [];
